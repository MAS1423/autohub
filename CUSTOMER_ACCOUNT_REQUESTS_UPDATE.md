# تحديث حسابات العملاء وطلبات السيارات

## ملخص التحديث

أصبحت خدمة **«أرسل طلب سيارة»** مرتبطة بحساب عميل. لا يمكن للزائر إرسال طلب مجهول؛ إذ يتطلب النظام إنشاء حساب مبسط يحفظ بيانات التواصل والطلبات في لوحة مستقلة للمستخدم.

| العنصر | السلوك الجديد |
|---|---|
| تسجيل العميل | الاسم ورقم واتساب مطلوبان، والبريد الإلكتروني اختياري. |
| إرسال طلب سيارة | يتطلب جلسة مستخدم مصادق عليها، وتُستخدم بيانات الحساب تلقائيًا. |
| سجل العميل | يظهر الطلب الأصلي مرة واحدة في `/account` مع رقم الطلب، المحتوى، المعارض المستهدفة، والتاريخ. |
| المفضلة | تُفصل المفضلة محليًا لكل حساب في المتصفح حتى لا تختلط مفضلة المستخدمين. |
| لوحة الإدارة | تحتوي على تبويب **المستخدمون** وتبويب **طلبات السيارات** مع محتوى كل طلب ومعلومات مرسله. |

> في بيئة التشغيل المحلية، الدخول اللاحق للعميل يتم برقم واتساب المسجل. لا توجد خدمة رسائل SMS أو تحقق واتساب حقيقية في النسخة المحلية.

## مسارات الاستخدام

| المسار | الغرض |
|---|---|
| `/vehicle-request` | إنشاء حساب العميل عند الحاجة ثم إرسال طلب السيارة. |
| `/account` | لوحة العميل: البيانات، الطلبات المرسلة، والمفضلة. |
| `/login` | دخول المعرض/الإدارة بالبريد وكلمة المرور، أو دخول العميل برقم واتساب. |
| `/admin` | لوحة الإدارة: المستخدمون وطلبات السيارات المنظمة. |

## التحقق المحلي

بعد فك الضغط، شغّل الأوامر التالية داخل مجلد المشروع:

```powershell
pnpm install --frozen-lockfile
Copy-Item .env.example .env
pnpm seed
pnpm dev
```

ثم افتح `http://localhost:3000/vehicle-request` كزائر جديد. أدخل الاسم ورقم واتساب، ثم أنشئ الحساب وأرسل طلبًا. ستظهر تفاصيله في `/account`، وباستخدام حساب المدير ستظهر في `/admin` ضمن تبويب **طلبات السيارات**.

## حسابات العرض

| الدور | البريد | كلمة المرور |
|---|---|---|
| المدير | `admin@autohub.sa` | `admin123` |
| المعرض | `dealer@autohub.sa` | `dealer123` |
| مستخدم تجريبي | `user@autohub.sa` | `user123` |

يمكن للعميل الجديد تسجيل الدخول من تبويب **عميل** عبر رقم واتساب المسجل عند إنشاء حسابه.

## نتائج التحقق

تم تنفيذ فحص الأنواع، والاختبارات، وبناء الإنتاج بنجاح. تشمل الاختبارات التحقق من منع إرسال طلب سيارة من مستخدم غير مسجل وحفظ الطلب داخل سجل العميل بعد توزيعه على المعارض المعتمدة.
