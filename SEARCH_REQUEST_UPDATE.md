# تحديث البحث وطلبات السيارات

تمت إضافة تجربة بحث سيارات جديدة إلى AutoHub، مع الحفاظ على تشغيل المشروع محليًا دون خدمة قاعدة بيانات خارجية.

| الميزة | السلوك الحالي |
|---|---|
| البحث الأساسي | ثلاث قوائم منسدلة: الماركة، ثم الطراز/النوع، ثم سنة الصنع. |
| ترابط القوائم | تبقى قائمة الطراز معطلة حتى اختيار الماركة، ثم تعرض طرازات الماركة المختارة فقط. |
| سنة الصنع | قائمة منسدلة واحدة لسنة الصنع بدل نطاق السنوات. |
| الفئة | حقل للفئة أو التجهيز، مثل `GXR` أو `فل كامل`. |
| السعر | نطاق واضح من–إلى، أو ميزانية قصوى واحدة. |
| البحث الحر | يبحث في الماركة والطراز ونوع المركبة والفئة. |
| طلب سيارة | نموذج رسالة يُوزع على المعارض المعتمدة والنشطة فقط. |
| التنقل العام | تم إخفاء الخريطة والمدينة والأسعار للتركيز على الشراء المباشر والطلب عن بُعد. |
| الدخول المحلي | تعمل الحسابات التجريبية عبر HTTP المحلي، كما يمكن اختيار الحساب لملء بياناته تلقائيًا في صفحة الدخول. |
| المفضلة | زر واضح باسم «المفضلة» مع عداد في الشريط العلوي وحالة «ضمن المفضلة» في بطاقات السيارات وصفحة التفاصيل. |
| مقارنة السيارات | زر واضح باسم «قارن السيارات» مع عداد من 3، وشريط يوضح السيارات المختارة والإجراء التالي. |
| استمرارية المقارنة | تُحفظ قائمة المقارنة محليًا وتبقى عند التنقل بين الصفحات أو إعادة التحميل. |

## التشغيل المحلي

```powershell
pnpm install --frozen-lockfile
Copy-Item .env.example .env
pnpm dev
```

بعد ظهور رسالة التشغيل، افتح `http://localhost:3000`.

## الحسابات التجريبية

| الدور | البريد الإلكتروني | كلمة المرور |
|---|---|---|
| مدير النظام | `admin@autohub.sa` | `admin123` |
| حساب معرض | `dealer@autohub.sa` | `dealer123` |
| مستخدم تجريبي | `user@autohub.sa` | `user123` |

> يمكن النقر على الحساب المطلوب من صفحة `/login` لملء بياناته تلقائيًا. إذا كانت لديك بيانات محلية قديمة، نفّذ `pnpm seed` ثم أعد تشغيل الخادم.

## مسارات التجربة

| المسار | الغرض |
|---|---|
| `/search` أو `/vehicles` | البحث عن السيارات بالقوائم المنسدلة للماركة والطراز وسنة الصنع، مع فلاتر إضافية اختيارية. |
| `/vehicle-request` | إرسال طلب سيارة إلى المعارض المعتمدة. |

## التحقق

```powershell
pnpm seed
pnpm check
pnpm test
pnpm build
```

> يعيد `pnpm seed` تهيئة بيانات العرض المحلية، وهو مناسب قبل أي تجربة نظيفة للطلبات والنتائج.
