# إصلاح لوحة المعرض وتسجيل الخروج

تعالج هذه النسخة مشكلتين في التشغيل المحلي: الوصول إلى لوحة تحكم المعرض وإنهاء جلسة المستخدم.

| المجال | الإصلاح المنفذ |
|---|---|
| لوحة المعرض | تم التحقق من ربط حساب المعرض التجريبي بمعرض الجزيرة للسيارات، وتظهر لوحة التحكم في المسار `/dashboard` بعد الدخول. |
| حماية اللوحة | عند محاولة فتح `/dashboard` بعد الخروج، تظهر شاشة «تسجيل الدخول مطلوب» بدل بيانات المعرض. |
| الدخول من شاشة الحماية | زر الدخول في شاشة الحماية يفتح `/login?redirect=%2Fdashboard` ليرجع المستخدم إلى اللوحة بعد المصادقة. |
| تسجيل الخروج داخل اللوحة | أضيف زر واضح باسم «تسجيل الخروج» في الشريط الجانبي، منفصل عن رابط «العودة للموقع». |
| تسجيل الخروج من الشريط العلوي | أصبحت القائمة المكتبية وقائمة الهاتف تنهيان الجلسة ثم توجّهان المستخدم إلى صفحة الدخول. |

## التشغيل المحلي

```powershell
pnpm install --frozen-lockfile
Copy-Item .env.example .env
pnpm seed
pnpm dev
```

بعد تشغيل الخادم، افتح `http://localhost:3000/login` واختر **حساب معرض** أو أدخل البيانات التالية:

| الدور | البريد الإلكتروني | كلمة المرور |
|---|---|---|
| حساب معرض | `dealer@autohub.sa` | `dealer123` |

بعد الدخول، يُفتح المسار `/dashboard`. استخدم زر **تسجيل الخروج** في أسفل الشريط الجانبي للتحقق من إنهاء الجلسة.

## التحقق المنفذ

تم اختبار دخول حساب المعرض، فتح لوحة معرض الجزيرة، تسجيل الخروج، محاولة العودة المباشرة إلى اللوحة بعد الخروج، والانتقال إلى صفحة الدخول المحلية. كما نجحت الأوامر `pnpm check` و`pnpm test` و`pnpm build`، مع 15 اختبارًا ناجحًا.
