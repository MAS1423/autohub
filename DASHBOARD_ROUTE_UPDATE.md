# إصلاح توجيه إدارة المعرض

تعالج هذه النسخة خطأ توجيه حساب المعرض إلى الصفحة العامة بدل لوحة الإدارة.

| الحالة | السلوك بعد الإصلاح |
|---|---|
| دخول حساب معرض | يستقبل الخادم وجود معرض مرتبط بالحساب ويعيد `postLoginPath` إلى `/dashboard`. |
| رابط إعادة توجيه قديم | حتى إن احتوى الرابط على مسار صفحة المعرض العامة، يُعاد مالك المعرض إلى لوحة الإدارة بعد تسجيل الدخول. |
| قائمة حساب المعرض | يظهر خياران منفصلان: **إدارة معرضي** للوحة التحكم و**عرض صفحة المعرض العامة** للصفحة التي يراها العملاء. |
| الرابط داخل اللوحة | يسمى رابط الصفحة العامة صراحةً «عرض الصفحة العامة للمعرض» حتى لا يختلط مع الإدارة. |
| تسجيل الخروج | يبقى زر تسجيل الخروج واضحًا ويعيد المستخدم إلى صفحة الدخول، مع منع الوصول إلى اللوحة بعد انتهاء الجلسة. |

## التشغيل المحلي

```powershell
pnpm install --frozen-lockfile
Copy-Item .env.example .env
pnpm seed
pnpm dev
```

افتح `http://localhost:3000/login` وسجّل الدخول بحساب المعرض:

| البريد الإلكتروني | كلمة المرور |
|---|---|
| `dealer@autohub.sa` | `dealer123` |

بعد الدخول، اختر **إدارة معرضي** من قائمة المستخدم. يجب أن تكون الوجهة `/dashboard`.

## التحقق المنفذ

تم اختبار الدخول عبر `/login?redirect=%2Fdashboard`، واختبار رابط يحاكي التحويل الخاطئ إلى الصفحة العامة (`/login?redirect=%2Fdealer%2Fal-jazeera-motors`). في الحالتين وصل حساب المعرض إلى `/dashboard`. كما تم اختبار زر «إدارة معرضي» من القائمة ونجح. اجتازت النسخة أوامر `pnpm check` و`pnpm test` و`pnpm build` مع 15 اختبارًا ناجحًا.
