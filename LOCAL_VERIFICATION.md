# تقرير التحقق المحلي — AutoHub

تمت مراجعة المشروع المرفق وإعداده للعمل محليًا بصورة مكتفية ذاتيًا. تعتمد النسخة الافتراضية على ملف JSON محلي يُنشأ تلقائيًا، لذلك يمكن تصفح المعارض والسيارات وتسجيل الدخول وإرسال الاستفسارات دون MySQL أو S3 أو خدمات صور خارجية.

| المجال | الحالة بعد الإصلاح | التحقق المنفذ |
|---|---|---|
| بدء التطبيق | يعمل دون ملف `.env` ودون MySQL في الوضع الافتراضي `DATA_MODE=local`. | شُغّل الخادم محليًا في وضعي التطوير والإنتاج. |
| البيانات التجريبية | تتولد تلقائيًا في `data/autohub.local.json` وتحتوي حسابات ومعارض وسيارات تجريبية. | نُفذ `pnpm seed` بنجاح. |
| الاختبارات | جميع الاختبارات ناجحة. | `pnpm test`: عدد 11 اختبارًا ناجحًا. |
| سلامة الأنواع | لا توجد أخطاء TypeScript. | `pnpm check` ناجح. |
| بناء الإنتاج | اكتمل بناء العميل والخادم بنجاح. | `pnpm build` ناجح. |
| صور العرض | جميع صور الواجهة المضمنة محلية ومساراتها تعمل، ولا توجد روابط صور Unsplash أو GitHub في مكونات الواجهة. | فُحصت الصفحات الرئيسية والمعارض والسيارات والتفاصيل والخريطة، وتأكد تحميل الصور من `/assets/*`. |
| رفع الصور | تُحفظ الصور المرفوعة محليًا وتُخدم عبر `/uploads/*` مع نوع MIME صحيح. | رُفع ملف JPEG تجريبي وتحقق الرد من `Content-Type: image/jpeg`. |
| الخريطة | لا تتعطل الصفحة دون مفتاح Google Maps؛ تظهر خريطة محلية تفاعلية قابلة للنقر. | فُحصت صفحة `/map` في نسخة الإنتاج دون مفتاح. |

## التشغيل الموصى به

نفّذ `pnpm install --frozen-lockfile` ثم `pnpm dev` من داخل مجلد المشروع. راجع `README.md` للحصول على خطوات التشغيل والحسابات التجريبية وخيار التحويل إلى MySQL عند الحاجة.
